<%= link_to 'Back', @project, class: 'btn btn-secondary mb-3' %>
<h1>Editing task</h1>

<%= render 'form' %>

<div class="card">
  <div class="card-body">
    <h2 class="card-title mb-4">
     <span>Editing Project</span></h2>


<%= render 'form' %>
</div>
</div>
